# Bean & Brew Project

This project is a Next.js 16 project with TypeScript and Tailwind v4.

## Verification

To verify the project is structurally sound and type-safe:

```bash
timeout 120 npm run type-check
timeout 60 npm run lint
timeout 120 npx vitest run
timeout 300 npm run build
```
